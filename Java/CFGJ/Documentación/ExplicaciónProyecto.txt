1.  El problema que tenía era manipular la cola de partidos pendientes, si en el proceso de disputa hubiera algun problema, por ejemplo que algun jugador tuviera sanción o similar, la cola de partidos se vería afectada aun no resolviendo el caso (sea disputado o no disputado), ví más simple poder trabajar con una copia revisando con peek, realizar las comprobaciones pertinentes y luego poder registrar el resultado si no se hiciera catch de alguna invalidez en el partido, luego llamo a la clase gestora liga y con el metodo de poner en cola partidos pendientes le meto un poll y se resuelve como partido disputado pasando por parametro la propia variable que he creado en el método para definir "partido". Así maneja la cola de partidos pendientes y permanecer allí mismo si ocurre algun error y disputarlo luego cuando se solvente

2. Como dije en mi memoria, el primer prototipo de validación duplicaba la misma lógica, entendiendo que el mismo código podría servir para ambos casos de distintas clases, no hizo falta usar instanceof ya que al crear un método privado como auxiliar para presentar el presupuesto como la clase padre/original PersonaLiga, no haría falta distinguir entre objetos que hereden de la misma clase, delego en el método calcularCosteMensual que era un principio polimórfico entre todas las clases, con esto únicamente, era capaz de poder validar a las 3 clases y no crear una validación única para cada una.


3. Si no sobreescribia podía tener varios resultados idénticos del personaje creando una instancia distinta por cada una de ellas, es decir podía tener varias estadisticas repetidas por personaje, siendo esto un error lógico ya que los personajes son solo uno y la estadística debería ir ligada al uso entre un jugador y un personaje (de momento solo existe el winrate), para ello se sobreescribe equals y hashCode en la clase PersonajeJuego y que utilicen el nombre del personaje y no la dirección de memoria, entonces al crear distintos objetos de PersonajeJuego, como por ejemplo: Pepe juega Kali, Javi juega Kali, serían la misma clave "Kali" y no duplica entradas en el HashMap. Este proceso se puede observar en la Clase Jugador metodo registrarResultado, donde toca el HashMap para las estadisticasPorPersonaje y la clave es el personaje y el valor las estadísticas