package modelo;
// Enum que define por defecto los roles de los jugadores, no pueden haber dos jugadores TITULARES con el mismo rol en el equipo, no se plantea con los suplentes

public enum Rol {
    TOP, JUNGLE, MID, ADC, SUPPORT
}
