


INNER JOIN + LEFT JOIN + WHERE 

-- Selecciona los usuarios que tengan una suscripción VIP (Activa o caducada) y que no sean entrenadores. Usando INNER JOIN + LEFT JOIN + WHERE 

SELECT CONCAT(u.nombre, ' ', u.apellido) AS UsuariosVIPFROM usuario u 
INNER JOIN socio s ON u.id_usuario = s.id_socio
INNER JOIN membresia m ON s.id_membresia = m.id_membresia
LEFT JOIN entrenador e ON u.id_usuario = e.id_entrenador
WHERE m.tipo = 'Anual VIP' 
AND e.id_entrenador IS NULL;


INNER JOIN + GROUP BY + HAVING + COUNT 

-- Dime el nombre y correo de los usuarios (Activos) que tienen una membresía anual que siga activa actualmente. INNER JOIN + GROUP BY + HAVING + COUNT 

SELECT u.nombre, u.email, m.tipo, m.estado FROM usuario u  
INNER JOIN socio s ON u.id_usuario = s.id_socio
INNER JOIN membresia m ON s.id_membresia = m.id_membresia
WHERE m.tipo LIKE 'Anual%' AND m.estado = 'Vigente';


INNER JOIN + Funciones agregadas 

-- ¿Cuántas clases hay y cuál es el cupo máximo total de todas ellas?



INNER JOIN + GROUP BY + HAVING + Subconsulta 

-- Selecciona el socio que más clases practica.

LEFT JOIN + INNER JOIN (3×) + GROUP BY 

-- Dime la cantidad de equipamiento que no está operativo en las clases de Boxeo.

INSERT INTO SELECT 

-- Crea una tabla de auditoría llamada Pagos_Altos e inserta todos los pagos superiores a 100€ con los datos del socio que los realizó.

UPDATE con Subconsulta 

-- Actualiza el estado de la membresía a 'Próxima a caducar' para todos los socios cuya membresía vence en los próximos 30 días.

DELETE con Subconsulta 

-- Elimina todas las asistencias de socios que tienen membresía suspendida.

Subconsulta anidada compleja 

-- Encuentra los entrenadores que imparten clases a las que asisten socios que no son VIP, agrupándolos por especialidad y mostrando cuántos socios no VIP tienen en total.

JOINs múltiples + agregaciones + HAVING 

-- Lista las clases que utilizan equipamiento en reparación, junto con el total de asistentes y el nombre del entrenador, ordenadas por cantidad de asistentes.